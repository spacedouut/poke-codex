[project]
name = "poke-codex-mcp"
version = "0.1.0"
description = "FastMCP bridge for Poke tunnel + Codex CLI orchestration"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=3.2.4",
    "poke>=0.2.1",
]

[project.scripts]
poke-codex-mcp = "main:main"
